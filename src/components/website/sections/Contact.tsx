import React from 'react';
import { Page } from '../Page';
import { Header } from '../Header';

export function Contact() {
  return (
    <Page id="contact" style={{ background: 'hsl(320, 30%, 65%)' }}>
      <Header>Contact Us</Header>
    </Page>
  );
}
